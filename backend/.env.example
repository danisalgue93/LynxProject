PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Production/default database. Use this with `npm run prisma:push && npm run seed`.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lynx_market?schema=public

# For immediate local UI testing without DB credentials, leave STORE_DRIVER=memory.
# Set STORE_DRIVER=prisma once DATABASE_URL is valid and migrations are pushed.
STORE_DRIVER=memory

SOLANA_CLUSTER=devnet
SOLANA_RPC_URL=https://api.devnet.solana.com
PROGRAM_ID=CiKuW8r71WnTLkGAKvFyYhtV2UhuJ4j8swDPDc8PEXvu
LYNX_MINT=
TREASURY_WALLET=LYNX_DEV_TREASURY
ADMIN_API_TOKEN=
